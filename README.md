# Dark-theme-landing-page-figma

 Some svgs downloaded are bugged so I reused the same svg everywhere, wherever I could


 Removed the container on body restricting the content. Used sections instead of divs to separate them into different useable chunks. Used inline paddings. Added generic names. Tried to make it as pixel-y related to figma as possible
